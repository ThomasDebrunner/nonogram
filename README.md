# Simple C++ Nonogram Solver

This is a simple nonogram solver written in C++. It is a command line application that takes a file as input and outputs the solution to the console.

## Usage

```
cmake .
make
./nonogram <input_file>
```
